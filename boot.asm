[org 0x7c00]
mov ah, 0x02    ; Читаємо диск
mov al, 15      ; Беремо одразу 15 секторів про запас
mov ch, 0
mov dh, 0
mov cl, 2       ; Починаємо з другого сектора (де наше ядро)
mov bx, 0x1000  ; Куди кладемо ядро в оперативку
mov es, bx
xor bx, bx
int 0x13

jmp 0x1000:0000 ; Стрибаємо в ядро!

times 510-($-$$) db 0
dw 0xaa55